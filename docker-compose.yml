version: "3.9"

services:
  research:
    build: .
    container_name: ${PROJECT_NAME}
    tty: true
    stdin_open: true
    volumes:
      - ./:/repo
      - ${HOME}/.codex:/root/.codex
      - ${HOME}/.ssh:/root/.ssh
      - ${HOME}/.gitconfig:/root/.gitconfig
